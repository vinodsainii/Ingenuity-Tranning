// in javascript function are first class citizen;
// 1.function can be assign to a variable
// 2. function can be use as a arguments
// 3. can also use as return SVGPatternElement.




// function can be use as a arguments
// a callback is function thatis exevuted after function as finished exevutin hense


var students = {
    {name: 'John', age: "13"},
    {name: 'John2', age: "14"},
};

function admitstudent(student){
    setTimeout(() => {
        students.forEach((student) => {
            str+=student.name+" ";});
            console.log(str)
    }, 100);
}

function displayStudent() {
    setTimeout(() => {
        va
        students.forEach((student) => {
            str+=student.name+" ";});
}


var nseStudent ={name}